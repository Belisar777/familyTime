{
  "name": "familytimes",
  "version": "0.1.0",
  "private": true,
  "description": "Rodinný organizační systém FamilyTimes",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
